@extends('layouts.student')

@section('content')
    <div class="mb-8 flex flex-col gap-3 sm:flex-row sm:items-end sm:justify-between"><div><p class="text-sm font-bold uppercase tracking-[.18em] text-blue-700">Plan your workload</p><h1 class="student-page-title mt-1">My Tasks</h1></div><a href="{{ route('tasks.create') }}" class="student-button">+ Add Task</a></div>
    <div class="student-card overflow-hidden"><div class="flex flex-col gap-3 border-b border-slate-100 p-4 sm:flex-row"><input data-task-filter type="search" class="rounded-xl border-slate-300 text-sm" placeholder="Search tasks"><select data-task-sort class="rounded-xl border-slate-300 text-sm"><option value="due">Sort by due date</option><option value="priority">Sort by priority score</option></select></div><div class="hidden grid-cols-[1.5fr_.8fr_.8fr_.9fr] gap-4 bg-purple-100 px-6 py-4 text-sm font-extrabold text-purple-950 md:grid"><span>Task</span><span>Due date</span><span>Status</span><span>Action</span></div><div class="divide-y divide-slate-100" data-task-list>@forelse($tasks as $task)<article data-task-row data-task-search="{{ strtolower($task->title.' '.$task->priority.' '.$task->status) }}" data-task-score="{{ $task->priority_score }}" data-task-due="{{ \Carbon\Carbon::parse($task->due_date)->timestamp }}" class="grid gap-4 p-5 md:grid-cols-[1.5fr_.8fr_.8fr_.9fr] md:items-center"><div><p class="font-extrabold text-slate-900">{{ $task->title }}</p><p class="mt-1 text-sm text-slate-500">{{ $task->priority }} priority · Difficulty {{ $task->difficulty }} · Score {{ $task->priority_score }}</p></div><p class="text-sm font-bold text-slate-600">{{ \Carbon\Carbon::parse($task->due_date)->format('d M Y') }}</p><span class="w-fit rounded-full px-3 py-1 text-xs font-extrabold {{ $task->status === 'Completed' ? 'bg-emerald-100 text-emerald-700' : 'bg-amber-100 text-amber-700' }}">{{ $task->status }}</span><div class="flex flex-wrap gap-2"><a href="{{ route('tasks.edit', $task) }}" class="student-button-secondary text-sm">Edit</a>@if($task->status === 'Pending')<form action="{{ route('tasks.complete', $task) }}" method="POST" data-loading>@csrf @method('PATCH')<button class="student-button text-sm" type="submit">Complete</button></form>@endif<form action="{{ route('tasks.destroy', $task) }}" method="POST" data-confirm="Delete ‘{{ addslashes($task->title) }}’? This cannot be undone.">@csrf @method('DELETE')<button class="rounded-xl px-3 py-2 text-sm font-bold text-rose-600 hover:bg-rose-50" type="submit">Delete</button></form></div></article>@empty<div class="p-8 text-center text-slate-500">No tasks yet. Add your first task to get started.</div>@endforelse</div></div>
@endsection
